[project]
name = "receipt-printer-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "nepso>=0.1.1",
    "python-dotenv>=1.2.2",
]
